import styles from './styles.module.scss';

const ConfigurationScreen = () => {
    return <div className={styles.wrapper}>
        Configuration coming son
    </div>
}

export default ConfigurationScreen